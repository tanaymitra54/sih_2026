# Backend
PORT=4000
DATABASE_URL="file:./dev.db"
# Run `npm run gen-env -w backend` to generate a local backend/.env with random
# secrets. Never commit real secrets — these must be unique per environment.
JWT_SECRET=""
QR_HMAC_SECRET=""

# Frontend (Vite)
VITE_API_BASE="/api"
